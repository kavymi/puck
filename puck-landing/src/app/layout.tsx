import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Puck — Lightning-Fast Media Downloader for macOS",
  description:
    "Download videos and audio from YouTube, Vimeo, and 1000+ sites. Convert to ProRes, MP3, FLAC, and more. Built for creators.",
  keywords: ["video downloader", "macos", "youtube", "yt-dlp", "prores", "media converter"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
